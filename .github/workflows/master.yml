name: Grizzly Manager - Build Cura plugin
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Zip Folder
      run: zip -r GrizzlyManager.curaplugin .
    - name: Release to Github
      run: echo "Release"
